## 2026-06-04 - [Code Redundancy and UX Consistency]
**Learning:** Redundant method definitions in UI code (likely from merge conflicts or poor version control) can lead to inconsistent behavior and "zombie" features that don't update correctly. In this app,  and  were duplicated, causing confusion.
**Action:** Always scan for duplicate method names when working on UI files to ensure UX consistency and maintainability.
